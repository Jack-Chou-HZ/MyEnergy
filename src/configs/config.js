define([], function () {
  const configs = {
    PG_MONITOR: 'monitor', // the id of the container element of the index.html page
    PG_ABOUT: 'about',
    PG_HEADER: 'header',
    PG_FOOTER: 'footer',
    PG_APP: 'app'
  }
  return configs
})
